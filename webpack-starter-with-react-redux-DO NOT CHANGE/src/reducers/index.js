import { combineReducers } from '@reduxjs/toolkit'

export default combineReducers({
  dummyKey: () =>
    'Tricks redux into thinking it has valid reducer. Delete when you start adding reducers',
})
